# Copy to .env.local (development) or .env.production (deploy) and fill in values.
# Keep secrets out of version control; this file is committed as a template.
# cp .env.example .env.local

# Server-side Sentry DSN
SENTRY_DSN=

# Client-side DSN (public). Use same value as above.
NEXT_PUBLIC_SENTRY_DSN=

# Token for uploading source maps via Sentry build plugin
SENTRY_AUTH_TOKEN=

# Supabase credentials
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
